# 10x

@test "best-case: add and remove a pure-bash trap 10x" {
	bash --norc --noprofile -i tests/add_and_remove.bash 10
}

@test "add and remove a comity trap 10x" {
	bash --norc --noprofile -i tests/add_and_remove.bash 10 $PWD/comity.bash
}

@test "add and remove a comity1 trap 10x" {
	bash --norc --noprofile -i tests/add_and_remove.bash 10 $PWD/comity1.bash
}

@test "add and remove a comity2 trap 10x" {
	bash --norc --noprofile -i tests/add_and_remove.bash 10 $PWD/comity2.bash
}

@test "add and remove a comity3 trap 10x" {
	bash --norc --noprofile -i tests/add_and_remove.bash 10 $PWD/comity3.bash
}

@test "add and remove a comity4 trap 10x" {
	bash --norc --noprofile -i tests/add_and_remove.bash 10 $PWD/comity4.bash
}

@test "add and use a pure-bash trap 10x" {
	bash --norc --noprofile -i tests/add_and_use.bash 10
}

@test "add and use a comity trap 10x" {
	bash --norc --noprofile -i tests/add_and_use.bash 10 $PWD/comity.bash
}

@test "add and use a comity1 trap 10x" {
	bash --norc --noprofile -i tests/add_and_use.bash 10 $PWD/comity1.bash
}

@test "add and use a comity2 trap 10x" {
	bash --norc --noprofile -i tests/add_and_use.bash 10 $PWD/comity2.bash
}

@test "add and use a comity3 trap 10x" {
	bash --norc --noprofile -i tests/add_and_use.bash 10 $PWD/comity3.bash
}

@test "add and use a comity4 trap 10x" {
	bash --norc --noprofile -i tests/add_and_use.bash 10 $PWD/comity4.bash
}

# 100x

@test "best-case: add and remove a pure-bash trap 100x" {
	bash --norc --noprofile -i tests/add_and_remove.bash 100
}

@test "add and remove a comity trap 100x" {
	bash --norc --noprofile -i tests/add_and_remove.bash 100 $PWD/comity.bash
}

@test "add and remove a comity1 trap 100x" {
	bash --norc --noprofile -i tests/add_and_remove.bash 100 $PWD/comity1.bash
}

@test "add and remove a comity2 trap 100x" {
	bash --norc --noprofile -i tests/add_and_remove.bash 100 $PWD/comity2.bash
}

@test "add and remove a comity3 trap 100x" {
	bash --norc --noprofile -i tests/add_and_remove.bash 100 $PWD/comity3.bash
}

@test "add and remove a comity4 trap 100x" {
	bash --norc --noprofile -i tests/add_and_remove.bash 100 $PWD/comity4.bash
}

@test "add and use a pure-bash trap 100x" {
	bash --norc --noprofile -i tests/add_and_use.bash 100
}

@test "add and use a comity trap 100x" {
	bash --norc --noprofile -i tests/add_and_use.bash 100 $PWD/comity.bash
}

@test "add and use a comity1 trap 100x" {
	bash --norc --noprofile -i tests/add_and_use.bash 100 $PWD/comity1.bash
}

@test "add and use a comity2 trap 100x" {
	bash --norc --noprofile -i tests/add_and_use.bash 100 $PWD/comity2.bash
}

@test "add and use a comity3 trap 100x" {
	bash --norc --noprofile -i tests/add_and_use.bash 100 $PWD/comity3.bash
}

@test "add and use a comity4 trap 100x" {
	bash --norc --noprofile -i tests/add_and_use.bash 100 $PWD/comity4.bash
}

# 1000x


@test "best-case: add and remove a pure-bash trap 1000x" {
	bash --norc --noprofile -i tests/add_and_remove.bash 1000
}

@test "add and remove a comity trap 1000x" {
	bash --norc --noprofile -i tests/add_and_remove.bash 1000 $PWD/comity.bash
}

@test "add and remove a comity1 trap 1000x" {
	bash --norc --noprofile -i tests/add_and_remove.bash 1000 $PWD/comity1.bash
}

@test "add and remove a comity2 trap 1000x" {
	bash --norc --noprofile -i tests/add_and_remove.bash 1000 $PWD/comity2.bash
}

@test "add and remove a comity3 trap 1000x" {
	bash --norc --noprofile -i tests/add_and_remove.bash 1000 $PWD/comity3.bash
}

@test "add and remove a comity4 trap 1000x" {
	bash --norc --noprofile -i tests/add_and_remove.bash 1000 $PWD/comity4.bash
}

@test "add and use a pure-bash trap 1000x" {
	bash --norc --noprofile -i tests/add_and_use.bash 1000
}

@test "add and use a comity trap 1000x" {
	bash --norc --noprofile -i tests/add_and_use.bash 1000 $PWD/comity.bash
}

@test "add and use a comity1 trap 1000x" {
	bash --norc --noprofile -i tests/add_and_use.bash 1000 $PWD/comity1.bash
}

@test "add and use a comity2 trap 1000x" {
	bash --norc --noprofile -i tests/add_and_use.bash 1000 $PWD/comity2.bash
}

@test "add and use a comity3 trap 1000x" {
	bash --norc --noprofile -i tests/add_and_use.bash 1000 $PWD/comity3.bash
}

@test "add and use a comity4 trap 1000x" {
	bash --norc --noprofile -i tests/add_and_use.bash 1000 $PWD/comity4.bash
}
